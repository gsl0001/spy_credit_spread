[pytest]
markers =
    unit: single-module unit test (fast, no network)
    integration: multi-module / IO test
    reconcile: backtest ↔ live path reconciliation
    slow: takes > 1s
testpaths = tests
asyncio_mode = auto
